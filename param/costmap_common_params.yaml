obstacle_range: 5.0 # max_dist of newly added obstacle [m]
raytrace_range: 4.0 # max_dist of newly added free space [m]

footprint: [[-0.15, -0.15], [-0.15, 0.15], [0.15, 0.15], [0.15, -0.15]] # [m]
footprint_padding: 0.1
#robot_radius: 0.17

inflation_radius: 1 # how far from the obstacle will be non zero cost [m]
cost_scaling_factor: 2.5 # how steep the decrease will be, bigger = steeper

min_obstacle_height: 0.1 # TODO nemeneno, checknout
resolution: 0.025
#NavfnROS: # melo by byt true defaultne
#    allow_unknown: true

map_type: costmap

observation_sources: scan # point_cloud_sensor

#point_cloud_sensor: {
#sensor_frame: camera_depth_frame,
#data_type: PointCloud2,
#topic: /camera/depth/color/points, 
#marking: true,
#clearing: true}

scan: {
    sensor_frame: camera_depth_frame, # TODO je to ok? nema tam byt base_link? asi ne...
    data_type: LaserScan,
    topic: camera/scan,
    marking: true,
    clearing: true}
